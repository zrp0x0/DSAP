#include <bits/stdc++.h>
using namespace std;

int N, ans;

int main(void) {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> N;
    vector<pair<int, int>> V(N);
    for (int i = 0; i < N; i++) {
        cin >> V[i].first >> V[i].second;
    }
    sort(V.begin(), V.end());
    for (int i = 0; i < N; i++) {
        
    }
    return 0;
}