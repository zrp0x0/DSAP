# Stack

## 정의와 성질

### 스택의 정의
- 먼저 들어간 원소가 제일 나중에 나오게 됨
- First In Last Out (FILO)

### 스택의 성질
- 원소의 추가가 O(1)
- 원소의 제거가 O(1)
- 제일 상단의 원소 확인이 O(1)
- 제일 상단이 아닌 나머지 원소들의 확인/변경은 **원칙적**으로 불가능

---

## 기능과 구현

### 구현
```cpp
const int MX = 1000005;
int dat[MX];
int pos = 0;
```
- 야매식 구현
- pos가 음수가 되지 않게 잘 검사해야함
- pos값 자체가 현재 스택의 원소 수

### 실제 구현
```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAX = 1000005;
int dat[MAX]; // <bits/stdc++.h>에 std::data랑 충돌이 일어날 수 있음
int pos = 0;

void push(int x) {
    dat[pos] = x;
    pos++;
}

void pop() {
    if (pos > 0)
        pos--;
}

int top() {
    if (pos > 0)
        return dat[pos - 1];
    return -1;
}

void test() {
push(5); push(4); push(3);
    cout << top() << '\n'; // 3
    pop(); pop();
    cout << top() << '\n'; // 5
    push(10); push(12);
    cout << top() << '\n'; // 12
    pop();
    cout << top() << '\n'; // 10
}

int main() {
    test();
    return 0;
}
```

---

## STL Stack

### Method<type>
- void push(type v): 가장 위에 v를 넣음
- void pop(): 가장 위에 값을 제거함
- type top(): 가장 위에 값을 뽑아옴
- bool empty(): 비어있으면 true / 안 비어있으면 false
- std::size_t size(): 해당 스택의 현재 원소 개수를 반환함

### 주의사항
- 스택이 비어있는데 top(), pop()을 호출하면 런타임 에러가 발생함
- top()이나 pop()을 사용할 때는 항상 스택이 비어있는지 먼저 확인해야함

---

## 연습문제

### BOJ 10828
- 그냥 단순히 스택을 사용하면 됨

### BOJ 10773
- **최근**에 쓴 것을 지운다 -> 스택을 활용할 수 있다의 사고 방식

### BOJ 1874

### BOJ 2493
- 틀린 이유
    - 시간 초과
    - 배열로 선언해서 사실상 O(N^2)으로 처리함
    - 반복문 2개를 사용하여서 각 인덱스 별로 작은게 있는지 찾아보기

- 문제의 핵심
    - 왼쪽으로만 레이저를 날림 (한쪽으로 검사)
    - 제일 큰 것만 중요함
    - 부딪혔다면 남겨놔야함
    - 만약 이미 거대한게 있으면 거대한거 뒤에 뭐가 있든지 상관없음
    - 내가 처음에 한 방법은 이 원리를 적용함
        - 단, empty 처리를 함

- 더 효율적인 풀이 사용
    - empty를 넣지 말고 dummy로 제일 높은 건물과 0을 세움
    - 스택 두 개를 사용하지 말고, pair<height, number>로 작성
    - 만약 height가 앞에 있는 것보다 크다 -> 닿지 않음 -> pop
        - 여기서 핵심은 더미보다 클 수 없음 (문제의 범위 + 1)
        - 시간 복잡도 N을 보고 O(N^2)으로 문제를 접근하면 안된다는 사실을 인지해야함!!!
        - 더미보다 클 수 없으므로 일단 넣음
        - 넣고 난 후에 큰 값이 들어온다? 일단 뺌 pop() -> 만약 나보다 큰게 없으면 더미를 만남(0으로 출력) {100000001, 0}

- 좀 정리
    - 더미(보초병 기법)
    - pair 사용

- 근데 왜 스택으로 접근해야할까? 
    - 일단 시간 복잡도가 O(N)
        - push(N) + pop(N) => O(N)

    - 스택으로 접근한 이유
        - 1. 한 방향으로 검사함
        - 2. 특정 조건에 따라서 앞에 뭐가 있는지 보지 않아도 됨
        - 3. pop을 통해 삭제 / push를 통해 유지